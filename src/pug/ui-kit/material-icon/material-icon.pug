mixin material-icons({ type='material icon symbol', color, }={})
    - let colorModifier = ''

    case color
        when 'dark-shade'
            - colorModifer = 'material-icon__color-dark-shade'

        i.materrial-icons(class=`${colorModifier}`)= type

